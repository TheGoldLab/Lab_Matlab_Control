function messageString_ = getMsg(timeout)
% function messageString_ = getMsg(timeout)
%
% silly overload which allows UDP messaged to be captured locally instead
% of being sent to a remote machine

messageString_ = 'handshaknbake';