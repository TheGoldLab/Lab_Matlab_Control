function u_ = root(u_, varargin)% u_ = root(u_, varargin)%% Overloaded root method for class dXudp:%   set up or clean up system resources for a dXudp object%% Updated class instances are always returned.%% Usage:%   root(a, 'clear')  cleans up%   root(a, varargin) optional list of args to send to set%----------Special comments-----------------------------------------------%-%%-% Sets up and cleans up a 'dXudp' object, typically%-%   called by rAdd, rClear or rDone.%----------Special comments-----------------------------------------------%% Copyright 2005 by Joshua I. Gold%   University of Pennsylvania% if ~u_.available%     return% end% root(u_, 'clear') means clean up%   otherwise initializeif isempty(varargin) || ~(ischar(varargin{1}) && strcmp(varargin{1}, 'clear'))    %%    % INITIALIZE    % make it active by adding 'active' arg, unless    %   it's already there    clear matlabUDP    % call set with args    u_ = set(u_, varargin{:});else    %%    % CLEAN UP    % close matlabUDP mex utility    %u_.socketIndex = matlabUDP('close');end